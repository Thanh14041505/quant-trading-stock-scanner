# Sao chép thành .streamlit/secrets.toml (file này đã nằm trong .gitignore) hoặc dán vào
# Streamlit Community Cloud -> App settings -> Secrets. KHÔNG commit key thật.
VNSTOCK_API_KEY = "điền-key-của-bạn-ở-đây"
